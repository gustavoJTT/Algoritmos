#include <stdio.h> //biblioteca standart input and output, para que sejam possíveis inputs e outputs

int main() { //sempre declarar o tipo do main, se for void(vazio) não precisa do return, POR PADRÃO É MAIN
   printf("Hello World!\n");
   printf ("Meu primeiro programa em ANSI C\n"); //"\n" é uma quebra de linha
   return 0;   //padrão para mostrar q o programa terminou
}